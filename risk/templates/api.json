{
  "fields": [{{% for question in latest_question_list %}
      {
        "name": {{ question.label }},
        "type": {{ question.attr.attr_type }}, {% if question.value %}
        "value": {{ question.value }}{% endif %}
      }
  {% endfor %}}]
}
